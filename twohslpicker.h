#ifndef TWOHSLPICKER_H
#define TWOHSLPICKER_H

#include <QWidget>
#include "hslpicker.h"
#include <QGridLayout>
#include <QLabel>

/*
class TwoHSLPicker:public QWidget
{
    //color picker per 2 colori
    Q_OBJECT
public:
    TwoHSLPicker(QWidget* parent=0);
    HSLPicker* getColor1()const;
    HSLPicker* getColor2()const;
    QLabel* getLabel1()const;
    QLabel* getLabel2()const;
    ~TwoHSLPicker();

private:
    HSLPicker *color1,*color2;
    QLabel* label1,*label2;
    QGridLayout* grid;
signals:
    void color1Changed(QColor* c);
    void color2Changed(QColor* c);
};
*/

#endif // TWOHSLPICKER_H
